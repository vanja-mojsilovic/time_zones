Scenario
    Get a list of spot IDs from Jira, using API request and JQL "summary ~ "Go Live" AND status = QA"
    Get spot ID, company name, hours_of_operation, time_zone, zip code, latitude, longitude, metro_area_id, metro area name from Spothopper application.